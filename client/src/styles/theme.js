import { css } from "styled-components";

const colors = {
  gray: "rgb(214 215 215 / var(--tw-bg-opacity, 1))",
  blue: "rgb(18 168 191 / var(--tw-bg-opacity, 1))",
};

const fonts = {};

const theme = {
  colors,
  fonts,
};

export default theme;
